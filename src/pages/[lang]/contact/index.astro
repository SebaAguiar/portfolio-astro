---
import ContactContainer from '../../../components/containers/ContactContainer.astro';
import Layout from '../../../layouts/Layout.astro';
export const getStaticPahs = async () => {
	return [{ params: { lang: 'es' } }, { params: { lang: 'en' } }];
};
---

<Layout title='contact'>
	<ContactContainer />
</Layout>
