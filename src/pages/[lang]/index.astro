---
import LandingContainer from '../../components/containers/LandingContainer.astro';
import Layout from '../../layouts/Layout.astro';

export const getStaticPahs = async () => {
	return [{ params: { lang: 'es' } }, { params: { lang: 'en' } }];
};
---

<Layout title='hola'>
	<LandingContainer />
</Layout>
